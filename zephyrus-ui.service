# Copy to /etc/systemd/system/zephyrus-ui.service
# Edit User, WorkingDirectory, and ExecStart paths to match your setup
[Unit]
Description=Zephyrus UI
After=network.target

[Service]
Type=simple
# CHANGE THESE to match your user and install path:
User=YOURUSER
WorkingDirectory=/home/YOURUSER/zephyrus-ui
ExecStart=/home/YOURUSER/zephyrus-ui/venv/bin/uvicorn app:app --port 7000 --host 0.0.0.0
Restart=always
RestartSec=3
EnvironmentFile=-/home/YOURUSER/zephyrus-ui/.env

[Install]
WantedBy=multi-user.target
